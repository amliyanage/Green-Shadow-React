export interface Log {
    logCode: string
    cropDate: string
    observedImage: string
    logDate: string
    logDetail: string
    param : {
        fieldCode: string
        cropCode: string
        staffId: string
    }
}